# Minimal pip requirements for the wrapper/orchestrator layer.
# The heavy dependencies (vLLM, flash-attn, VERL, BFCL) have their own install
# paths and version constraints — see README §2. This file captures the
# "glue" packages that `unified_eval.py` and the scoring scripts import.

# Core orchestrator / I/O
pyarrow>=14.0
pandas>=2.0
numpy>=1.24
tqdm>=4.66

# HuggingFace (datasets loader for IFEval, tokenizers for templating)
transformers>=4.42
datasets>=2.19
huggingface_hub>=0.24

# IFEval / BFCL scoring helpers
nltk>=3.8
langdetect>=1.0.9
jinja2>=3.1
termcolor>=2.4

# OpenAI-compatible HTTP clients (MemAgent calls vllm serve via openai SDK)
openai>=1.99
requests>=2.31

# Vendor-specific — installed separately, listed here for reference:
#   vllm>=0.7.0            (pip install vllm; GPU wheels)
#   flash-attn>=2.7        (pip install flash-attn --no-build-isolation)
#   verl                   (custom checkout; see README §3-4)
#   bfcl-eval              (pip install bfcl-eval)  or vendored copy
